#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    int sum = 0;
    sum = x + x;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int y;
        y = 5;
        y++;
    }

    /*
    Number of iterations = 3.
    * O(5) + O(3*n) = O(n)
    if n < 100000 --> O(n)
    O(k*n) --> if k << n --> O(n)

    O(1) + O(4) + ... = O(1)  --> constant operation.

    * On online coding websites 1 sec = 10^7 iterations  -> remember.

    e.g.
    10^7 --> 1 sec.
    10^8 --> 10 sec.
    10^9 --> 100 sec.
    10^10 --> 1000 sec ~ 15 mins.




    */

    return 0;
}
